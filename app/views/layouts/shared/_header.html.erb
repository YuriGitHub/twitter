<!-- Fixed navbar -->
<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <div  class="navbar-brand">
                <%= link_to "Twitter", root_path %>
            </div>
            <% if user_signed_in? %>
                    <%= render "search/search" %>
            <% end %>
            <div id="navbar" class="collapse navbar-collapse">

              <div class="locale">

                <div class="dropdown">
                  <button class="dropbtn"><%= locale %></button>
                  <div class="dropdown-content
">
                    <%= link_to 'en', locales_path(:locale => 'en') %>
                    <%= link_to 'ru', locales_path(:locale => 'ru') %>
                  </div>
                </div>

              </div>

                <ul class="nav navbar-nav navbar-right">
                    <% if user_signed_in? %>
                        <li class="dropdown">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                                <%= current_user.login %> <span class="caret"></span></a>
                            <ul class="dropdown-menu">
                              <li><%= link_to (t '.my_page'), user_path(current_user) %> </li>
                              <li role="separator" class="divider"></li>
                              <li><%= link_to (t '.edit_account'), edit_user_registration_path %> </li>
                              <li><%#= link_to "Create Post", new_car_path %></li>
                              <li role="separator" class="divider"></li>
                              <li> <%= link_to (t '.logout'), destroy_user_session_path, :method => :delete %></li>
                            </ul>
                        </li>
                        <%# else %>
                        <li><%#= link_to 'Sign In', new_user_session_path %></li>
                    <% end %>
                </ul>
            </div><!--/.nav-collapse -->
        </div>
    </div>
</nav>
