
<% @user.photo_albums.each do |album| %>
    <%= link_to album.name, user_photo_album_path(@user, album) %></br>
<% end %>
<% if @user.id == current_user.id %>

<% end %>


<%= link_to 'Add New Photo Album', new_user_photo_album_path(@user), class:'btn btn-default' %>